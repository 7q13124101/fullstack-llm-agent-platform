# fullstack_llm_agent_platform - Production Environment
# Generated by fastapi-fullstack v
#
# IMPORTANT: Copy this file to .env.prod before deploying
#   cp .env.prod.example .env.prod
#
# WARNING: Never commit .env.prod to version control!

# === Container Prefix (must be unique per server) ===
CONTAINER_PREFIX=fullstack_llm_agent_platform
# === Domain Configuration ===
DOMAIN=example.com
ACME_EMAIL=admin@example.com
# === PostgreSQL ===
POSTGRES_USER=postgres
POSTGRES_PASSWORD=  # Generate: openssl rand -base64 32
POSTGRES_DB=fullstack_llm_agent_platform
# === JWT Secret ===
SECRET_KEY=  # Generate: openssl rand -hex 32
# === Traefik Dashboard ===
# Generate with: htpasswd -nb admin yourpassword
# Example: admin:$apr1$xyz$hash
TRAEFIK_DASHBOARD_AUTH=
# === Messaging Channels ===
# Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
CHANNEL_ENCRYPTION_KEY=TELEGRAM_WEBHOOK_BASE_URL=https://api.example.com